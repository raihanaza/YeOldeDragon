matter Coffee {
    init(roast: int) {
        ye.roast: int = roast + 1;
    }

    don brew() -> void {
        proclaim("This coffee is called ${ye.roast} and it is a ${ye.roast}.");
    }

    don isSeasonal() -> string {
        return "hello world!";
    }
}