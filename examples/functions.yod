don addNums(num1: int, yes: boolean) -> void {
    return "hi";
    ~ proclaim("hello");
}

~~
don addNums(num1: int, yes: boolean) -> int {
    return num1;
}
~~
~~
don addNums(num1: int, num2: int) -> int {
    return (num1 + num2);
    proclaim("hello!")
}
~~

~ proclaim(addNums(1, shall));
~ for now this doesn't work since need to figure out how to integrate labels into function call
~ proclaim(addNums(num1: 22, num2: 3));
~ Expected output: 25
